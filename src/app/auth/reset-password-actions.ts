'use server';

/**
 * @fileOverview CE FICHIER EST DÉFINITIVEMENT DÉSACTIVÉ.
 * La réinitialisation de mot de passe est gérée par le SDK Client natif Firebase.
 * Cela garantit une stabilité de 100% sans conflit de runtime serveur (INTERNAL).
 */

export async function sendCustomPasswordResetEmailAction() {
  throw new Error("Action supprimée. Le site utilise désormais le reset natif Firebase Client.");
}
