'use server';

/**
 * @fileOverview CE FICHIER EST DÉFINITIVEMENT DÉSACTIVÉ.
 * La logique de reset password est désormais gérée par le SDK Client natif Firebase
 * pour garantir 100% de fiabilité et éviter les erreurs de runtime serveur (INTERNAL).
 */

export async function sendCustomPasswordResetEmailAction() {
  throw new Error("Action supprimée. Le site utilise désormais le reset natif Firebase.");
}
